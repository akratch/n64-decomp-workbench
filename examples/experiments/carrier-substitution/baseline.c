int build_value(int width, int height) {
    static int carrier;
    static int blocker_a;
    static int blocker_b;
    int value = width + (carrier * 0);
    if (blocker_a) {}
    if (blocker_b) {}
    if (carrier) {}
    return value + height;
}
